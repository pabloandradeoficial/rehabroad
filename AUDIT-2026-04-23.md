# Auditoria Rehabroad — 2026-04-23

## O que foi corrigido nesta auditoria

### Segurança
- **[Crítico] XSS no chat RehabFriend**: `RehabFriendChat.tsx` usava regex-based markdown render + `dangerouslySetInnerHTML` sem escape. Qualquer HTML no output da IA executaria. Substituído por `react-markdown` (já estava nas deps).
- **[Médio] XSS latente em BlogPost**: `formatContent` (mesmo padrão inseguro). Safe hoje (dados estáticos) mas viraria XSS se blog fosse servido por API. Substituído por `react-markdown`.
- **[Baixo] JSON-LD schema sem escape**: `BibliotecaClinicaPage.tsx` agora escapa `<` como `<` para prevenir script-tag breakout.
- **[Médio] PII em log**: `forum.ts:149` logava `user.email` em `console.error`. Agora só `user.id`.
- **[Baixo] Check admin redundante**: `admin.ts` `/admin/students` usava checagem inline em vez do `adminMiddleware`. Consolidado.

### Dependências
- `npm audit fix` resolveu 7 vulnerabilidades.
- Remoção de `react-snap` (unused) e `@cloudflare/vite-plugin` (unused devDep) resolveu **18 adicionais**.
- **Vulnerabilidades: 31 → 6** (restantes: uuid via svix via resend, e hono major bump).

### Dead code
- Deletados 19 arquivos mortos (components, pages, hooks, data, layout). Build e typecheck continuam passando.
- 15 arquivos de UI primitives (`accordion.tsx`, `tooltip.tsx`, etc.) foram mantidos deliberadamente — padrão shadcn, baratos de manter para uso futuro.

### Code quality
- `eslint --fix` resolveu 17 problemas auto-corrigíveis.
- Removidos 3 `eslint-disable` órfãos (em `PatientDashboard`, `worker/index.ts`; o terceiro estava em `shared/types.ts` que foi deletado).
- Removido import `Next` não usado em `helpers.ts`.
- Removida var `const user` não usada em `comite.ts:144`.
- Limpas linhas-fantasma deixadas pelo `eslint --fix` em 7 arquivos.

### Build config
- Removido `chunkSizeWarningLimit: 5000` de `vite.config.ts`. Agora Vite avisa quando chunks passam de 500KB em vez de esconder o problema.

### Resultado final
- TypeScript: **limpo** ✓
- Build: **passa em 11.9s** ✓
- ESLint: de 52 → 35 problemas (17 errors + 18 warnings — todos `any`/`hook-deps`/`no-empty`, precisam de tipagem manual).
- Vulnerabilidades: 31 → 6.
- Arquivos no projeto: -19.

---

## Recomendações (por ordem de impacto)

### 1. [ALTO] Expandir cobertura de testes

**Correção do achado inicial:** existem 6 testes E2E em `e2e/fluxos-criticos.spec.ts` (financeiro, fórum, agenda, prontuário, NeuroFlux). Meu scan inicial só olhou em `src/` e errei.

**Adicionado nesta sessão:** Vitest configurado + 35 testes unitários em `src/worker/lib/helpers.test.ts` cobrindo `isOwnerAdminEmail`, `extractTokenFromCookieValue`, `sanitizeBaseUrl`, `getEnvString`, `normalizeDelimitedTextValue`, `splitDelimitedText`, `getInsertedId`, `normalizeCaminhoRecord`. Coverage em helpers.ts: 38% statements, 52% funções. Rodar: `npm run test`.

**Próximo (ainda faltando):**

**Onde começar (ordem):**
1. **Auth + pagamento** (E2E Playwright): login Google, checkout Stripe happy path, webhook verification
2. **Worker routes** (unit/integration com `@cloudflare/workers-types` + miniflare): patients CRUD, HEP flow, auth middleware
3. **Componentes críticos** (Vitest + Testing Library): AuthGuard, PremiumGate, ErrorBoundary

**Alvo:** 30% coverage em 2 semanas com foco nos paths que já são bug-prone. Não persiga 100%.

### 2. [ALTO] Bundle size

Build atual:
- `App` chunk: **541KB** (first-load)
- `BibliotecaClinicaPage`: 337KB
- `BarChart` (recharts): 356KB
- `Exportacao`: 430KB

Ações:
1. **Lazy-load `recharts`**: dashboards usam charts pesados. `const BarChart = lazy(() => import('recharts').then(m => ({ default: m.BarChart })))`. Payload cai só onde precisar.
2. **Split `BibliotecaClinicaPage`**: provavelmente está importando data estático grande. Mover data para fetch lazy ou gerar páginas estáticas.
3. **Split god components**: 6 arquivos >1000 linhas (StudentHub 1537, Agenda 1531, NeuroFlux 1485, patients.ts 1495, PatientDetail 1194, Painel 1199). Split por tab/rota/feature — reduz bundle inicial e facilita testes.
4. **Remover ou lazy `jspdf` + `html2canvas`**: 200KB+ só pra exportação. Dynamic import no clique do botão.

### 3. [ALTO] Terminar remoção de vulnerabilidades

6 vulns restantes, todas exigem upgrade major:
```
resend (6.x → 7.x) — rompe API
svix (transitive via resend)
uuid (transitive via svix)
hono (4.7.7 → 4.12.14)
```

Passo: ler changelog de cada, atualizar individualmente, testar as rotas afetadas (webhook Stripe + emails Resend). **Não rode `npm audit fix --force` cego** — lê a migration.

### 4. [MÉDIO] Eliminar os 24 `any` restantes

Breakdown:
- `ComitePanel.tsx`: 5× `any` em handlers de agentes. Tipar com os tipos do router.
- `worker/lib/helpers.ts:434` `normalizeCaminhoRecord`: generics precisam constraint melhor.
- Espalhado em handlers de rotas worker.

**Padrão a seguir:** para response bodies, criar tipos em `src/shared/api.ts` (criar esse arquivo) e compartilhar entre worker e react-app. Você já tinha `shared/types.ts` (deletei porque estava vazio/unused) — reaproveitar essa estrutura.

### 5. [MÉDIO] Corrigir 6 warnings de `react-hooks/exhaustive-deps`

Cada uma é potencial bug de stale closure. Inclua a dep no array OR tire a função para fora do componente OR memoize com `useCallback`. Risky de auto-fix porque pode mudar comportamento — um por um.

### 6. [MÉDIO] Considerar limpar os 15 UI primitives não usados

Knip listou `accordion.tsx`, `alert-dialog.tsx`, `avatar.tsx`, `collapsible.tsx`, `field.tsx`, `input-group.tsx`, `popover.tsx`, `radio-group.tsx`, `scroll-area.tsx`, `separator.tsx`, `skeleton.tsx`, `switch.tsx`, `table.tsx`, `tooltip.tsx`, `dropdown-menu.tsx` (parcial).

Deixei para você decidir: se o plano é "vou usar essas nas próximas features", mantém. Se é "copiei shadcn inteiro e nunca vou usar", deleta (`rm` + `npm uninstall radix-ui` das partes órfãs).

### 7. [MÉDIO] God components: split

Candidatos prioritários (muitas responsabilidades num arquivo só):
- `StudentHub.tsx` (1537 linhas) — separar por seção: progresso, casos, comunidade
- `dashboard/Agenda.tsx` (1531) — separar calendário, drawer de agendamento, lista
- `dashboard/NeuroFlux.tsx` (1485) — separar motor de consulta, histórico, sugestões
- `worker/routes/patients.ts` (1495) — separar por recurso (patients, evaluations, evolutions, alertas)

Padrão: criar diretório `src/react-app/pages/student/hub/` com `index.tsx` + `_components/`, mover pieces.

### 8. [BAIXO] Observabilidade

Hoje: só `console.error`. Perde rastreabilidade em prod.
- Integrar **Cloudflare Workers Logs** (já nativo) ou **Logtail** para retenção + query
- Adicionar request-id correlation: middleware que põe `crypto.randomUUID()` no header e loga em tudo
- Monitorar Stripe webhook failures (atualmente só `console.error` — falha silenciosa em prod)

### 9. [BAIXO] Política de conteúdo confiável no chat da IA

Mesmo com `react-markdown` (escape HTML por default), a IA pode output prompts injection, links maliciosos, ou instruções perigosas sobre tratamento clínico. Considerar:
- Lista de domínios permitidos em links (via `remark-external-links`)
- Filter de palavras (termos clínicos incorretos, medicamentos sem contraindicação)
- Disclaimer explícito: "respostas da IA não substituem avaliação profissional" (já tem? confirmar)

### 10. [BAIXO] Cleanup complementar

- Remover os 92 exports não usados listados pelo knip (anima-se com os que estão em `data/` e `hooks/`)
- Consolidar `src/data/` e `src/react-app/data/` — dois diretórios `data/` é confuso
- Adicionar `CLAUDE.md` de projeto (atualmente só tem skill routing) com: stack, como rodar, como deployar, convenções de nome de arquivo
- Criar `CHANGELOG.md` (atualmente não existe) — útil para você mesmo

---

## Itens que NÃO tocaram os seus 4 arquivos em progresso

`AuthGuard.tsx`, `DashboardLayout.tsx`, `index.html`, `.netlify/netlify.toml` — sua WIP continua intocada. Quando for commitar, recomendo:

```bash
# Ver todos os arquivos modificados
git status

# Separar commits por categoria:
# 1. Sua WIP
git add src/react-app/components/AuthGuard.tsx src/react-app/components/layout/DashboardLayout.tsx index.html .netlify/netlify.toml
git commit -m "seu-commit-da-wip"

# 2. Auditoria - segurança
git add src/react-app/components/RehabFriendChat.tsx src/react-app/pages/BlogPost.tsx src/react-app/pages/BibliotecaClinicaPage.tsx src/worker/routes/forum.ts src/worker/routes/admin.ts
git commit -m "security: fix XSS in chat + blog, redact PII, consolidate admin check"

# 3. Auditoria - deps + dead code
git add package.json package-lock.json
git add -u  # pega os deletes
git commit -m "chore: remove 31→6 vuln deps, delete 19 dead files"

# 4. Auditoria - lint/config
git add -A
git commit -m "chore: eslint --fix, surface bundle-size warning"
```

---

*Gerado por auditoria automatizada Claude em 2026-04-23. Build: passa em 11.9s. Typecheck: limpo.*
